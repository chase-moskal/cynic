{
  "name": "cynic",
  "version": "0.0.1",
  "description": "Chainable runtime assertions; Type checking in JavaScript",
  "main": "build/Cynic.js",
  "scripts": {
    "build": "tsc && echo Build complete.",
    "tape": "tape build/tests/**/*.js",
    "test": "npm run tape --silent | faucet"
  },
  "keywords": [],
  "author": "Chase Moskal",
  "license": "MIT",
  "devDependencies": {
    "blue-tape": "^0.2.0",
    "faucet": "0.0.1",
    "typescript": "^1.8.9"
  }
}
